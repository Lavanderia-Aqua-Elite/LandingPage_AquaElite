// Importa los modulos necesarios

// Exporta por defecto y crea el componente
export default function MinTitle({ mintitle }) {
    return(
        <div className="conteinerMin">
            <h4 className="minTitle">{mintitle}</h4>
        </div>
    )
} 