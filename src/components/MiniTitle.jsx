// Importa los modulos necesarios

// Exporta por defecto y crea el componente con el hook
export default function MiniTitle( {minititle} ) {
    return(
        <div className="conteinerMin">
            <h3 className="miniTitle">{minititle}</h3>
        </div>
    )
}